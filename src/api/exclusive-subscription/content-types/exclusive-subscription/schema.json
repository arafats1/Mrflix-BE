{
  "kind": "collectionType",
  "collectionName": "exclusive_subscriptions",
  "info": {
    "singularName": "exclusive-subscription",
    "pluralName": "exclusive-subscriptions",
    "displayName": "Exclusive Subscription",
    "description": "Monthly exclusive XXX content subscriptions"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "subscriber": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::users-permissions.user"
    },
    "amount": {
      "type": "integer",
      "required": true
    },
    "paymentMethod": {
      "type": "enumeration",
      "enum": ["mtn_momo", "airtel_money", "pesapal"],
      "required": true
    },
    "paymentPhone": {
      "type": "string",
      "required": true
    },
    "transactionId": {
      "type": "string"
    },
    "status": {
      "type": "enumeration",
      "enum": ["active", "expired", "cancelled", "pending"],
      "default": "pending",
      "required": true
    },
    "startDate": {
      "type": "datetime",
      "required": true
    },
    "endDate": {
      "type": "datetime",
      "required": true
    },
    "pesapalTrackingId": {
      "type": "string"
    }
  }
}
